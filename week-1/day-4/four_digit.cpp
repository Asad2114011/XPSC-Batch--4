#include<bits/stdc++.h>
using namespace std;
int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    string s;
    cin>>s;
    int x=4-s.size();
    while(x--)
    {
        cout<<0;
    }
    cout<<s;
    return 0;
}